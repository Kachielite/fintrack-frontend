import React from "react";
import { CategoryEnum } from "@/core/common/interface/enums";

interface CategoryBadgeProps {
  category: CategoryEnum;
}

export default function CategoryBadge(_props: CategoryBadgeProps): null {
  return null;
}
