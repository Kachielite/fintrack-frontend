import React from "react";
import { InsightType } from "@/features/insights/insights.interface";

interface AdvisorCardProps {
  message: string;
  type?: InsightType;
}

export default function AdvisorCard(_props: AdvisorCardProps): null {
  return null;
}
