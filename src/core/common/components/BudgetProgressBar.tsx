import React from "react";
import { BudgetStatus } from "@/features/budgets/budgets.interface";

interface BudgetProgressBarProps {
  percentage: number;
  status: BudgetStatus;
}

export default function BudgetProgressBar(
  _props: BudgetProgressBarProps,
): null {
  return null;
}
