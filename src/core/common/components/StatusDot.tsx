import React from "react";
import { TransactionStatus } from "@/features/transactions/transactions.interface";

interface StatusDotProps {
  status: TransactionStatus;
}

export default function StatusDot(_props: StatusDotProps): null {
  return null;
}
