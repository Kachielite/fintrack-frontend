import React from "react";

interface SectionHeaderProps {
  title: string;
  action?: { label: string; onPress: () => void };
}

export default function SectionHeader(_props: SectionHeaderProps): null {
  return null;
}
